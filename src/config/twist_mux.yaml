twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic:   '/cmd_vel_teleop'
        timeout: 0.5
        priority: 10
      safety:
        topic:   '/cmd_vel_safety'
        timeout: 0.2
        priority: 100 